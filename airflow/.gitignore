# Local environment — copy .env.example to .env and fill in your values
.env

# Airflow runtime logs — generated on first run, can grow large
logs/

# Generated DAG and task files written by the DataOps Orchestrator at runtime
# The directories are tracked via .gitkeep; the files inside are not
dags/*.py
dags/tasks/*.py

# Hand-authored DAGs that must be tracked despite the rule above
!dags/gx_validate_dataset.py

# macOS
.DS_Store
